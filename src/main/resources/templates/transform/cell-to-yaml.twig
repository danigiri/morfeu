$( import 'macros.twig' as m -)$
$(- set meta = cell.cellModel.Metadata -)$
$(- set i = indentation -)$
$[- i -]$
$(- set d =  meta.getDirectivesFor(case) -)$
$(- if d.isPresent() -)$
	$(if  not d.get().contains("ATTRIBUTES-ONLY") )$$[ cell.name ]$:$( set i = concat(i,'  ') )$
	$(- else -)$
# ($[cell.name]$) directives: $[d.get().size()]$
	$(- endif -)$
$(- endif -)$
$#- now for the attributes -#$
$(- if cell.isComplex )$
$( for a in cell.asComplex.attributes.asList -)$
$[- i -]$$[a.name]$: $[a.value]$
$(- endfor )$
$#- and the children -#$

$(- endif -)$
