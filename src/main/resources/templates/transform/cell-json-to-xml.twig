{% set name = node.name.asText -%}
{%- set value = node.value.asText -%}
{%- if node.children.size==0 and value.length==0 %}
{{i}}<{{ name }} {%for a in node.internalAttributes %} {{a.name.textValue}}="{{a.value.textValue}}" {% endfor -%} 
			{%for a in node.attributes %} {{a.name.textValue}}="{{a.value.textValue}}" {% endfor -%} 
/>{% else -%}
{{i}}<{{ name }} 	{%for a in node.internalAttributes %} {{a.name.textValue}}="{{a.value.textValue}}" {% endfor -%}
			{%for a in node.attributes %} {{a.name.textValue}}="{{a.value.textValue}}" {% endfor -%} 
>{{value | xmlc}}
{% for childnode in node.children %}{% include "cell-json-to-xml.twig" with {'node': childnode, 'i':concat(i,'\t')} only %}{% endfor %}
{{i}}</{{ name }}>{% endif %}
