{#- if we are empty -#}{% if c.isSimple %}{% if c.getValue.isPresent %}{% else %} {}{% endif -%}
{%- elseif c.isComplex -%}
	{%- if (not (c.asComplex.attributes.hasAttributes)) and (not (c.asComplex.children.hasChildren)) %} {}{% endif -%}
{%- endif %}