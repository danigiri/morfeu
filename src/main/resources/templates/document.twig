{
	"name": "$[v.Name]$"
	,"uri": "$[v.URI]$"
	,"desc": "$[v.Desc]$"
	,"kind": "$[v.Kind]$"
	,"modelURI": "$[v.ModelURI]$"
	,"fetchableModelURI": "$[v.FetchableModelURI]$"
	,"contentURI": "$[v.ContentURI]$"
	,"valid": $[v.isValid]$
	$( if defined(problem) )$
		,"problem": "$[ problem ]$"
	$( endif )$
}