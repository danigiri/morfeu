{
	"name": "$[v.Name]$"
	,"desc": "$[v.Desc]$"
	,"kind": "$[v.Kind]$"
	,"modelURI": "$[v.ModelURI]$"
	,"contentURI": "$[v.ContentURI]$"
	,"valid": $[v.isValid]$
	$( if defined(problem) )$
		,"problem": "$[problem]$"
	$( endif )$
}